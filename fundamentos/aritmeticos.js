const [a,b,c,d] = [3,5,1,15]

const soma = a+b+c+d // Operação binária com dois operandos.
const subtracao = d - b
const multiplicacao = a * b
const divisao = d/a
const modulo = a % 2 // Muito utilizado para determinar se números são pares ou não.

console.log(soma, subtracao, multiplicacao, divisao, modulo)