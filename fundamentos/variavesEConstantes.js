var a = 3;
let b = 4;

var a = 30;
b = 40; // Variáveis criadas com let não podem ser mudadas recriando elas com let.

console.log(a, b);

const c = 5; // Valor não pode ser mudado, priorizar a utilização de constantes.

console.log(a, b, c);