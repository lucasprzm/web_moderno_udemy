const resultado = nota => nota >= 7 ? 'Aprovado' : 'Reprovado' // Expressão, se verdadeiro retorna aprovado, se não, reprovado. Semelhante a if e else.

console.log(resultado(7.1))
console.log(resultado(6.7))

