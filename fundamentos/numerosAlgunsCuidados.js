console.log(7 / 0); // Retorna infinito;
console.log("10" / 2); // Realiza a operação, mas com vírgula não dá certo.
console.log("Show!" * 2) // Não dá certo, algumas linguagens sim.
console.log(0.1 + 0.7); // O valor não fica correto, específico do IEEE, para agilizar o cálculo.
// console.log(10.toString()); // Dá erro.
console.log((10.345).toFixed(2)); //Dentro dos parênteses funciona.
console.log("3" + 2); // Concatena ao invés de somar, pois faz sentido o sinal de soma como concatenação.