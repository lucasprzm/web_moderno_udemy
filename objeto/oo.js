// Código não executável!!

// Procedural

processamento (valor1, valor2, valor3)

// Orientação a Objeto

objeto = {
    valor1,
    valor2,
    valor3,
    processamento () {
        // ...
    }
}
// objeto funciona como cápsula, com atributos e comportamentos que se relacionam dentro do objeto. Objeto interage com outro, pode ter um objeto dentro do outro, pode ter herança.
objeto.processamento() // Foco passou a ser o objeto

// Princípios importantes:
// 1. Abstração : como abstrair o que você precisa para dentro do seu programa.
// 2. Encapsulamento: interface de comunicação do usuário com a máquina. Esconder detalhes de implementação sem mudar a interface do usuário.
// Baixo nível de encapsulamento é ruim, dependência entre objetos elevada.
// 3. Herança (prototype): recebe características herdadas. Priorizar composição ao invés de herança.
// 4. Polimorfismo: conceito genérico como carro, celular, que possuem vários modelos e marcas. 